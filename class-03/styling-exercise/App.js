import React from "react";
// import "./styles.css"; <-- No olvides descomentar esto!

export function App() {
  return <div className="container">{/* Tu caja aqui! */}</div>;
}

// Recuerda que las props `style={}` recibe un objeto, como `style={ {color: 'red' } }
// En JSX se usa `className` en lugar de `class`
// puedes usar flexbox, grid layout, position: absolute, lo que quieras
